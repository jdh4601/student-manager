{
  "name": "student-manager",
  "private": true,
  "scripts": {
    "test": "npm --prefix frontend test",
    "e2e": "npm --prefix frontend run e2e",
    "build": "npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev",
    "typecheck": "npm --prefix frontend run typecheck",
    "qa:backend": "cd backend && .venv312/bin/ruff check app tests && .venv312/bin/pytest -q",
    "qa:frontend": "npm --prefix frontend run typecheck",
    "qa:e2e": "cd backend && .venv312/bin/pytest -m integration -q",
    "qa": "npm run qa:backend && npm run qa:frontend"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0"
  }
}
